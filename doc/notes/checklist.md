- Do we need to replace the integer representation of epoch time
with an abstraction that hides the underlying data structure?
  
- Can we refactor the interactive loop so that it is clear where
in the sequence we are blocking for user input, and which side
effects are deliberately executed subsequent to that input?
